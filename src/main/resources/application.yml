
spring:

  datasource:
    url: jdbc:mysql://localhost:3306/hydropower?characterEncoding=utf-8
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

  thymeleaf:
    cache: false

  mvc:
   view:
    prefix: /templates
    suffix: .html


# 映射端口
server:
  port: 8080
